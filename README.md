# vite-node-sourcemap-support
Apply SourceMap to code execution in vite-node

This implementation is a workaround.
It may be better to build .ts using a tool such as rollup (with typescript compiler) or parcel.
